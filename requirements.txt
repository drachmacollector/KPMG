pandas
openpyxl
playwright
beautifulsoup4
PyMuPDF
opencv-python
Pillow
ollama
tqdm
# paddlepaddle-gpu must be installed separately via the CUDA wheel:
#   pip install paddlepaddle-gpu==2.6.2 -i https://www.paddlepaddle.org.cn/packages/stable/cu118/
# Requires cuDNN 8 DLLs (installed via nvidia-cudnn-cu11 to avoid system-wide CUDA dependencies):
nvidia-cudnn-cu11==8.9.5.29
# PaddleOCR 2.x is pinned below 3.0 because PaddleOCR 3.x requires PaddlePaddle 3.x,
# which is incompatible with the paddlepaddle-gpu 2.6.2 CUDA wheel.
"paddleocr<3.0"
# New Google Gen AI SDK (provides `from google import genai`).
# NOTE: paddlepaddle-gpu 2.6.2 requires protobuf<=3.20.2 but google-genai
# needs protobuf>=5.x.  The conflict is resolved at runtime via:
#   os.environ["PROTOCOL_BUFFERS_PYTHON_IMPLEMENTATION"] = "python"
# set in ocr_engine.py before any protobuf import.  The pip warning is safe to ignore.
google-genai
google-search-results
python-dotenv
rapidfuzz