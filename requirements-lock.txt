# requirements-lock.txt  (GPU / dev machine)
# Manually curated from the actual imports in the codebase.
# Compatible with Python 3.12 and 3.13 (and 3.11 if still installed).
# Regenerate by running the app, not by `pip freeze` (which captures the entire venv).
#
# Direct dependencies:
#   verify_colleges.py  -> pandas, playwright, beautifulsoup4, tqdm, python-dotenv
#   document_processor.py -> PyMuPDF, opencv-python, Pillow
#   extractor.py        -> ollama
#   ocr_engine.py       -> paddlepaddle-gpu, paddleocr<3.0, tqdm
#   web_resolver.py     -> google-genai, rapidfuzz, python-dotenv
#   gui/app/runner.py   -> psutil, pywebview
#   gui/app/main.py     -> pywebview
#
# NOTE: paddlepaddle-gpu is the GPU wheel used on the dev machine.
#       For client/CPU-only machines use requirements-lock-cpu.txt instead.
# NOTE: paddlepaddle-gpu 2.6.2 requires protobuf<=3.20.2 but google-genai needs
#       protobuf>=5.x.  Resolved via PROTOCOL_BUFFERS_PYTHON_IMPLEMENTATION=python
#       set in ocr_engine.py before any protobuf import.

# ---------------------------------------------------------------------------
# Core pipeline — direct imports
# ---------------------------------------------------------------------------
pandas==3.0.2
openpyxl==3.1.5
playwright==1.60.0
beautifulsoup4==4.15.0
PyMuPDF==1.27.2.3
opencv-python==4.6.0.66
Pillow==12.2.0
ollama==0.6.2
tqdm==4.68.3
paddleocr==2.7.3
paddlepaddle-gpu==2.6.2
google-genai==2.10.0
rapidfuzz==3.14.5
python-dotenv==1.2.2

# ---------------------------------------------------------------------------
# NVIDIA CUDA runtime (GPU mode — dev machine only)
# ---------------------------------------------------------------------------
nvidia-cublas-cu11==11.11.3.6
nvidia-cuda-nvrtc-cu11==11.8.89
nvidia-cudnn-cu11==8.9.4.25

# ---------------------------------------------------------------------------
# PaddleOCR transitive dependencies
# ---------------------------------------------------------------------------
numpy==1.26.4
shapely==2.1.2
pyclipper==1.4.0
lmdb==2.2.1
imgaug==0.4.0
python-bidi==0.6.10
PyYAML==6.0.2
lxml==6.1.1
ruamel.yaml==0.19.1
Cython==3.2.8
six==1.17.0
decorator==5.3.1
packaging==26.1

# ---------------------------------------------------------------------------
# google-genai transitive dependencies
# ---------------------------------------------------------------------------
google-auth==2.55.1
google-api-core==2.31.0
googleapis-common-protos==1.75.0
proto-plus==1.28.0
protobuf==5.29.6
grpcio==1.81.1
grpcio-status==1.71.2
httpx==0.28.1
httpcore==1.0.9
h11==0.16.0
anyio==4.14.1
sniffio==1.3.1
pydantic==2.13.4
pydantic_core==2.46.4
annotated-types==0.7.0
typing_extensions==4.15.0
tenacity==9.1.4
websockets==16.0
distro==1.9.0

# ---------------------------------------------------------------------------
# google-auth transitive dependencies
# ---------------------------------------------------------------------------
pyasn1==0.6.3
pyasn1_modules==0.4.2
cachetools==7.1.4
cffi==2.0.0
pycparser==3.0
cryptography==49.0.0

# ---------------------------------------------------------------------------
# aiohttp (google-genai optional async transport)
# ---------------------------------------------------------------------------
aiohttp==3.14.1
aiosignal==1.4.0
aiohappyeyeballs==2.6.2
frozenlist==1.8.0
propcache==0.5.2
attrs==26.1.0
multidict==6.7.1
yarl==1.24.2

# ---------------------------------------------------------------------------
# pandas / Excel transitive dependencies
# ---------------------------------------------------------------------------
et_xmlfile==2.0.0
python-dateutil==2.9.0.post0
pytz==2026.2
tzdata==2026.1

# ---------------------------------------------------------------------------
# playwright transitive dependencies
# ---------------------------------------------------------------------------
pyee==13.0.1
greenlet==3.5.3

# ---------------------------------------------------------------------------
# beautifulsoup4 transitive dependencies
# ---------------------------------------------------------------------------
soupsieve==2.8.4

# ---------------------------------------------------------------------------
# Shared HTTP stack
# ---------------------------------------------------------------------------
requests==2.34.2
urllib3==2.7.0
certifi==2026.6.17
charset-normalizer==3.4.7
idna==3.18

# ---------------------------------------------------------------------------
# GUI (pywebview + pythonnet + psutil)
# pywebview uses the OS-native Edge WebView2 runtime on Windows.
# pythonnet is required by pywebview's edgechromium backend on Windows.
# ---------------------------------------------------------------------------
pywebview>=5.0
pythonnet>=3.0
psutil==7.2.2

# ---------------------------------------------------------------------------
# Logging / progress
# ---------------------------------------------------------------------------
colorama==0.4.6
colorlog==6.10.1
